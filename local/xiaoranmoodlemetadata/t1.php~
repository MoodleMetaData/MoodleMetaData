<?php
global $PAGE, $CFG, $DB;
        require_once('../../config.php');
        $PAGE ->set_title(get_string('pluginname','local_xiaoranmoodlemetadata'));
        echo "this is just a test";
 		  $numberq=mysql_query('SELECT count(*) from mdl_courseinfo');
 		  echo mysql_num_rows($numberq);
        if (mysql_num_rows($numberq)) {
				echo $numberq;
        }
?>
